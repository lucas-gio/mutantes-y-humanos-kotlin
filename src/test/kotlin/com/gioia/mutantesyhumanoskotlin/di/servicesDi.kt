package com.gioia.mutantesyhumanoskotlin.di

import org.kodein.di.DI

val testDI = DI {
/*    extend(di = di, copy = Copy {
        copy the binding<MongoDatabase>()
        copy the binding<MutantService>()
        copy the binding<StatsService>()
    })
*/
}